# =============================================================
# BASE REQUIREMENTS — Phase 1: Local Foundation
# =============================================================
# These are the ONLY dependencies for Phase 1.
# More packages will be added in later phases.

# Core Framework
Django>=5.2,<6.0
djangorestframework==3.15.2

# JWT Authentication (Phase 3)
# Simple, well-maintained JWT for DRF. Used by thousands of Django APIs.
# Java equivalent: jjwt / spring-security-oauth2-jose
djangorestframework-simplejwt==5.3.1

# PostgreSQL adapter (psycopg v3 — modern, async-capable)
# psycopg[binary] includes compiled C extensions for performance
psycopg[binary]>=3.2.10

# Environment variable management
# Reads from .env file and provides typed access
python-decouple==3.8

# Data Quality (Phase 6)
phonenumbers==8.13.39

# Background Processing (Phase 8)
celery==5.4.0
redis==5.0.4
